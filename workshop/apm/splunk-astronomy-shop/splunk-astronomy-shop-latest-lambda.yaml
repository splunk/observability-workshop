# Splunk Astronomy Shop Kubernetes Manifest - lambda
# Version: 2.0.6
# Generated on: 2026-07-16T11:22:15Z
#
# This manifest combines service deployments for the Splunk Astronomy Shop
# Services are defined in services.yaml
---

# === planning ===
# Kubernetes manifest for planning
# Auto-generated from opentelemetry-demo.yaml
# Contains 1 resource(s)
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: planning
  labels:
    opentelemetry.io/name: planning
    app.kubernetes.io/component: planning
    app.kubernetes.io/name: planning
    app.kubernetes.io/version: 2.0.6
    app.kubernetes.io/part-of: opentelemetry-demo
spec:
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      opentelemetry.io/name: planning
  template:
    metadata:
      labels:
        opentelemetry.io/name: planning
        app.kubernetes.io/component: planning
        app.kubernetes.io/name: planning
    spec:
      serviceAccountName: opentelemetry-demo
      containers:
      - name: planning
        image: ghcr.io/splunk/opentelemetry-demo/otel-planning:2.0.6
        imagePullPolicy: Always
        env:
        - name: WORKSHOP_ENV
          valueFrom:
            secretKeyRef:
              name: workshop-secret
              key: env
        - name: OTEL_SERVICE_NAME
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.labels['app.kubernetes.io/component']
        - name: NODE_IP
          valueFrom:
            fieldRef:
              fieldPath: status.hostIP
        - name: OTEL_COLLECTOR_NAME
          value: $(NODE_IP)
        - name: OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE
          value: cumulative
        - name: KAFKA_ADDR
          value: kafka:9092
        - name: KAFKA_TOPIC
          value: orders
        - name: KAFKA_GROUP_ID
          value: planning
        # Lambda configuration
        - name: LAMBDA_ENDPOINT
          value: "https://61d4vss237.execute-api.eu-west-1.amazonaws.com/demo/orders"
        - name: LAMBDA_CALL_INTERVAL_MINUTES
          value: "5"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: http://$(OTEL_COLLECTOR_NAME):4317
        - name: OTEL_PYTHON_LOG_CORRELATION
          value: 'true'
        - name: OTEL_RESOURCE_ATTRIBUTES
          value: service.name=$(OTEL_SERVICE_NAME),service.namespace=opentelemetry-demo,service.version=2.0.6,service.kafka=spanlink,deployment.environment=$(WORKSHOP_ENV)-lambda
        - name: SPLUNK_PROFILER_ENABLED
          value: 'true'
        - name: SPLUNK_PROFILER_MEMORY_ENABLED
          value: 'true'
        resources:
          limits:
            memory: 512Mi
          requests:
            memory: 256Mi
        volumeMounts: null
      initContainers:
      - command:
        - sh
        - -c
        - until nc -z -v -w30 kafka 9092; do echo waiting for kafka; sleep 2; done;
        image: busybox:latest
        name: wait-for-kafka
      volumes: null

---
