receivers:
    collectd:
      endpoint: "0.0.0.0:8081"

exporters:
    debug:
      verbosity: detailed
      file:
        path: ./agent.out 


service:
    pipelines:
      metrics:
        receivers: [collectd]
        exporters: [debug]